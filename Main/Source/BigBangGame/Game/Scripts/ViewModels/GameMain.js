define(['knockout'], function (ko) {
    var GameMain = function () {
        var gameTitle = "Mitt spel";

        return {
            gameTitle: gameTitle
        };
    }

    return GameMain;
});